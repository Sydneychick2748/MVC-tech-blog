{{!-- <!DOCTYPE html>
<html lang="en">
{{!-- this is the main.handlebars file --}}
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>The Tech Blog</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
 
<body>
    <div class="wrapper">
        <header>
            <h1>
                <a href="/">home</a>
            </h1>
            <h1>
               <a href="/dashboard">dashboard</a>
            </h1>
            <nav>
                {{!-- this is an if statement that    checks if the user is logged in --}}
                {{#if loggedIn}}
                <a href="/dashboard">dashboard</a>
                <button id="logout" class="btn-no-style logout">logout</button>
                {{else}}
                <a href="/login">login</a>
                {{/if}}
            </nav>
        </header>
        <div class="site-row">
            <div class="site-name">
                <h1>The Tech Blog</h1>
            </div>
        </div>
        <main>
            {{!-- this is the  body that is   passed in from the controller --}}
            {{{ body }}}
        </main>
    </div>

    {{#if loggedIn}}
    <script {{logout}} src="/javascript/logout.js"></script>
    {{/if}}
</body>

</html> --}}

<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>The Tech Blog</title>
  <link href="https://fonts.googleapis.com/css?family=IBM+Plex+Sans:400,400i,700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/style.css">
</head>

<body class="flex-column min-100-vh">
  <nav class="navbar">
    <ul>
      <li class="li-inline nav-item"><a href="/">Home</a></li>
      <li class="li-inline nav-item"><a href="/dashboard">Dashboard</a></li>
      <li class="li-inline nav-item"><a href="/login">Login</a></li>
      <li class="li-inline nav-item"><a href="#" id="logout-link">Logout</a></li>
    </ul>
  </nav>
  <header class="hero">
    <h1 class="app-title"><a href="/">The Tech Blog</a></h1>
  </header>

  <main class="col-auto">
    {{{body}}}
  </main>

  <footer class="flex-row justify-center">
    &copy; The Tech Blog
  </footer>
</body>

</html>
<script src="/js/logout.js"></script>




